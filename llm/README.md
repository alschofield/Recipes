# LLM Workspace

This folder is reserved for model evaluation, prompts, and fallback generation tooling.

Recommended future structure:

- `prompts/` for versioned prompt templates
- `evals/` for evaluation datasets and scripts
- `pipelines/` for generation/validation jobs
- `contracts/` if interface schemas diverge from docs

Reference contract docs:

- `../docs/llm-fallback-contract.md`
