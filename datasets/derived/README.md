# Derived Datasets

Derived artifacts created from raw datasets via deterministic transforms.

- `server-lib/` - migrated derived artifacts from former `server/lib/derived/`.

Keep transform provenance with each generated dataset version.
