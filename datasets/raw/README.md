# Raw Datasets

Source-of-truth dataset drops. Keep files immutable after import and add new versions as new files/directories.

- `server-lib/` - migrated raw sources from the former `server/lib/` location.
