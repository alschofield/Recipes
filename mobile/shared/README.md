# Mobile Shared Contract Assets

This folder is for shared non-runtime assets used by both native apps:

- API request/response examples
- event taxonomy references
- design token exports (if generated)
- QA fixtures

Do not place cross-platform runtime code here; iOS and Android remain pure native implementations.
